% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pf7k_utils.R
\name{pf7k_format_bia_for_stave}
\alias{pf7k_format_bia_for_stave}
\title{Convert Biallelic SNP Formats for Stave}
\usage{
pf7k_format_bia_for_stave(mut, gls = deparse(substitute(mut)))
}
\arguments{
\item{mut}{Character vector. A vector of SNP mutations in a biallelic format.}

\item{gls}{Character. The gene locus and SNP identifier, inferred from the input variable name by default.}
}
\value{
Character vector. Processed SNP data in a standardised format for \code{stave}. Missing values are returned as \code{NA}.
}
\description{
This function processes biallelic SNP data and converts it into a format compatible
with the \code{stave} tool. It ensures compatibility by handling phased, unphased,
and missing data, as well as removing redundant SNPs.
}
\details{
\subsection{Input format conventions:}{
\itemize{
\item \strong{Phased data}: Denoted by \verb{,}, converted to \code{|} in the output.
\item \strong{Unphased data}: Marked with \code{*}, adjusted to \code{/} for compatibility.
\item \strong{Missing data}: Represented by \code{-}, converted to \code{NA}.
\item \strong{Redundant SNPs}: Identical alleles (e.g., \code{G|G}) are simplified.
}
}

\subsection{Output format:}{
\itemize{
\item SNPs are prefixed with the gene locus in the form \code{gene:locus:SNP}.
\item Phased SNPs are joined with \code{|}.
\item Unphased SNPs are joined with \code{/}.
}
}
}
\examples{

# Example input with phased and unphased SNPs
crt_76 <- c("K,T", "K*,T", "T", "-") 
pf7k_format_bia_for_stave(crt_76)  

# Handles redundant and unphased SNPs
pf7k_format_bia_for_stave(c("G,G", "A,C", "A*|G"), gls = "dhps_581_a")  
}
