% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deduplicate.R
\name{deduplicate_pmid}
\alias{deduplicate_pmid}
\title{Deduplicate Studies by PMID with Conflicting Study Identifiers}
\usage{
deduplicate_pmid(df)
}
\arguments{
\item{df}{A data frame containing at minimum the following columns:
\itemize{
\item \code{pmid}: PubMed identifier
\item \code{study_ID}: Study identifier
\item \code{database}: Data source (e.g., WHO, GEOFF, Pf7k, WWARN)
\item Any additional fields required by \code{add_tags_diff_studyID()} (e.g., \code{publication_year})
}}
}
\value{
A list of data frames, each corresponding to a set of records with the same
\code{pmid} but different \code{study_ID}s. Each data frame includes a \code{keep_row} column
indicating which rows to retain (\code{"keep"}) or discard (\code{"remove"}).
}
\description{
Identifies groups of records that share the same PubMed ID (\code{pmid}) but have different
\code{study_ID}s, and applies a deduplication strategy to tag each row as \code{"keep"} or \code{"remove"}.
The deduplication logic is defined in the helper function \code{add_tags_diff_studyID()},
which applies a set of prioritized rules based on the \code{database} source.
}
